CREATE TABLE d6 (
	id int identity(0,1) primary key,
	answers varchar(26),
	group_id int,
)

CREATE TABLE d6_test (
	id int identity(0,1) primary key,
	answers varchar(26),
	group_id int,
)
